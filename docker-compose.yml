services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    working_dir: /app
    command: >
       sh -c "python manage.py migrate &&
              python manage.py runserver 0.0.0.0:8000"
