{
  "name": "Ajuda Tech webhook",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "ajuda-tech",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "webhook-v2",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [260, 300],
      "webhookId": "ajuda-tech-automation",
      "credentials": {}
    },
    {
      "parameters": {
        "jsCode": "const crypto = require('crypto');\nconst input = $json.body ?? $json;\nconst { event_id: eventId, message } = input;\nif (typeof eventId !== 'string' || !eventId.trim() || typeof message !== 'string' || !message.trim()) {\n  throw new Error('event_id and message are required');\n}\nconst payload = { event_id: eventId.trim(), message: message.trim() };\nconst signature = crypto.createHmac('sha256', $env.AUTOMATION_WEBHOOK_SECRET).update(JSON.stringify(payload)).digest('hex');\nreturn [{ json: { ...payload, signature } }];"
      },
      "id": "validate-sign",
      "name": "Validate and sign",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [500, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://app:8000/automation/webhook/",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {"name": "Content-Type", "value": "application/json"},
            {"name": "X-Automation-Signature", "value": "={{ $json.signature }}"}
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ { event_id: $json.event_id, message: $json.message } }}",
        "options": {"timeout": 70000}
      },
      "id": "app-request",
      "name": "Send to Ajuda Tech",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [760, 300]
    }
  ],
  "connections": {
    "Webhook": {"main": [[{"node": "Validate and sign", "type": "main", "index": 0}]]},
    "Validate and sign": {"main": [[{"node": "Send to Ajuda Tech", "type": "main", "index": 0}]]}
  },
  "active": false,
  "settings": {"executionOrder": "v1"},
  "versionId": "00000000-0000-0000-0000-000000000008",
  "meta": {"templateCredsSetupCompleted": true},
  "tags": []
}
